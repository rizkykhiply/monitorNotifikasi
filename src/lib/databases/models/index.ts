// Import All Models
import { default as auth } from './auth.model';
import { default as menu } from './menu.model';

// Assign All Models
const models = {
    auth,
    menu,
};

// Export Models
export { models };
