// Import All Models
import { default as auth } from './auth.model';
import { default as user } from './user.model';

// Assign All Models
const models = {
    auth,
    user,
};

// Export Models
export { models };
